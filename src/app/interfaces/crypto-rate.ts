export interface CryptoRate {
    usd: number
    php: number
    php1: number // this is the amount of 1 php against the currency
    axs?: number
    bnb?: number
    btc?: number
    eth?: number
    pvu?: number
}
